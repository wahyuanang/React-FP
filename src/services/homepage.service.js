import axiosInstance from "../api/axiosInstance";

const fetchDestination = async () => {
    try {
        
    } catch (error) {
        
    }
}

const fetchSearchDestination = async () => {
    try {
        
    } catch (error) {
        
    }
}

const fetchFilterDestination = async () => {
    try {
        
    } catch (error) {
        
    }
}

export default {
    fetchDestination,
    fetchFilterDestination,
    fetchSearchDestination
}